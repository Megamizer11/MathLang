git add .
git commit -m "commit description"
git push -u origin main

git pull origin main

git rm --cached *.i

git init
git remote add origin "https://github.com/Megamizer11/MathLang"
git branch -M main
git pull origin main --allow-unrelated-histories

git remote -v